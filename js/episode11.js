// Déclaration des variables
let nombre;

// Demande du nombre à l'utilisateur
nombre = prompt("Quel est le nombre ?");

// Affichage de la table de multiplication
for (let i = 1; i <= 10; i++) {
  console .log(nombre + " x " + i + " = " + nombre * i);
}
